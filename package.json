{
  "name": "mekobot",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && node ./out/pathMap",
    "start": "npm run build && node out/index",
    "dev": "nodemon -r tsconfig-paths/register ./src/index.ts",
    "test": "npm run build && jest test.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "oicq": "^2.2.3"
  },
  "devDependencies": {
    "@types/jest": "^29.0.3",
    "@types/node": "^18.0.0",
    "jest": "^29.0.3",
    "tsconfig-paths": "^4.1.0"
  }
}
